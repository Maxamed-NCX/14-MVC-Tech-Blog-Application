    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-post" class="button is-success is-rounded is-pulled-right">Create A New Post</button></a>
        
        <h2 class="title pb-4">dashboard: your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post| }}
           
        {{> post-info post}}
        <br>
    <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-danger is-outlined">Edit Post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">There are no Post currently</h6>

{{/if}}

<script src="/javascript/search.js"></script>

 